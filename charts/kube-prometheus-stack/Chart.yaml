apiVersion: v2
name: kube-prometheus-stack
description: Prometheus + Grafana + Alertmanager for the homelab
version: 0.1.0
dependencies:
  - name: kube-prometheus-stack
    version: "84.5.0"
    repository: https://prometheus-community.github.io/helm-charts
