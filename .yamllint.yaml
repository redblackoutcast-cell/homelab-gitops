extends: default
ignore: |
  charts/*/charts/
  charts/*/templates/
  charts/secrets/
rules:
  line-length:
    max: 200
    level: warning
  document-start: disable
  truthy:
    allowed-values: ["true", "false"]
    check-keys: false
